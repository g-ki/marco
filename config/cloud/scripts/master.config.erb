#!/bin/bash

wget -qO- https://get.docker.com/ | sh
sudo usermod -aG docker $(whoami)

docker pull george95/marco

docker swarm init --advertise-addr $(ifconfig eth1 | grep 'inet addr:' | cut -d: -f2 | awk '{ print $1}')
